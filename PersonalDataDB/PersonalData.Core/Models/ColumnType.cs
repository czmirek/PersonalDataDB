namespace PersonalData.Core
{
    public enum ColumnType
    {
        String,
        Integer,
        Double,
        Decimal,
        Boolean,
        DateTime,
        DateTimeWithOffset,
        ByteArray,
        ForeignKey
    }
}